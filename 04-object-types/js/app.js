// Your starting point, enjoy the ride!

// 1. Literal Object

// Declare variable to hold person object with properties firstname and lastname
const person = { firstname: 'Micha', lastname: 'Alt' };

// Query for article in DOM
const objectArticle = document.getElementById('object');

// Fill DOM
objectArticle.querySelector('li:first-child span').textContent = "...";
objectArticle.querySelector('li:last-child span').textContent = "...";
// objectArticle.querySelector('code').textContent =

// 2. Literal Object

// Declare variable to hold some fibonacci numbers

// Query for article in DOM

// Fill DOM
// with current array length
// with JSON code

// 3. Function Object

// Declare function to add two numbers

// Query for article in DOM

// Fill DOM
// with result of sample addition
// with function string
